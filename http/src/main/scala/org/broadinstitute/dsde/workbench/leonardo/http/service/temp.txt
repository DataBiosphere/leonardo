CreateApp API:

If enableCustomAppCheck is false, skip app allowlist check.

    If enableCustomAppCheck is true &Load project label

        if it has security-group label

        and value is high: use high-security APP allowlist

        else is NOT high, use default project allowlist.

    otherwise, if security-group label DNE, still check usergroup